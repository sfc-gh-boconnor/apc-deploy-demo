# profiles.yml — auto-generated by deploy_dbt.py
# Edit vars.yaml to change identity or pass -D flags to override.
default:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: "<YOUR_ACCOUNT>"
      user: "<YOUR_USER>"
      role: ACCOUNTADMIN
      database: APC_DEPLOY_DB
      warehouse: APC_DEPLOY_WH
      schema: DBT_ANALYTICS
      threads: 4
